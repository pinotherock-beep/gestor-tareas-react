export default function AboutPage() {
  return (
    <main className="container">
      <section className="card">
        <h2>Acerca del proyecto</h2>
        <p>Aplicación creada con React, React Router, Context API, Hooks, localStorage, animaciones CSS y pruebas unitarias.</p>
      </section>
    </main>
  );
}
